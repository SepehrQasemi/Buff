name: clouding-sweep
# DISABLED: self-hosted/Clouding sweep is out of service.

on: []

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Clouding sweep disabled
        run: echo "clouding-sweep is disabled."
